import React from 'react';
// import bs from 'bootstrap/dist/css/bootstrap.min.css';
import styles from './Weather.sass';


const Weather = () => (
  <header className={`${styles.container}`}>

  </header>
);

export default Weather; // eslint-disable-line
